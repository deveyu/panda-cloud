server:
  port: 9052
spring:
  application:
    name: panda-config-server
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/deveyu/config-reps
          default-label: ${spring.profiles.active}
          username: deveyu
          password: yudachang0307
eureka:
  instance:
    hostname: panda-config-server
  #    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://panda-server:9051/eureka/

